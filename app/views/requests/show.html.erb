<% @TMP_PROD_ID = 5%>
<div>
  <p><%=@request.name%></p>
  <p> 
    <% @request.product_list.each do |product_id|%>
      <%= product_id %>
    <% end %>
  </p>
</div>
<%= link_to 'Edit', edit_request_path(@request) %> |
<%= link_to 'Back', requests_path %> 
<%= link_to 'Add Product', 'http://localhost:3000/requests/' + @request.id.to_s + '/add_product?prod_id=5'%>